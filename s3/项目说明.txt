该测试项目用采maven+jpa+事务 其中maen 为maven -web-mc jpa为hibernate 事务采用注解的方式
在该项目中需要注意的问题有：
1.注意构建一般项目的流程即要建立什么样的项目有哪些配置文件一般来说
首先有一个针对整个maven项目的xml文件用于提供整个项目所需要的jar
其次针对具体的服务层要相应的配置文件xml比如该项目中针对hibernate和 事务 就要配置相应的xml文件待定这是因为maven一系列插件需要读取相应的配置文件
才能实现特定的功能
3针对项目测试过程中出现的一些问题：
首先是事务配置文件中为了方便起见因为采用的是基于注解的事务管理在该文件中定义好命名空间后提供一系列bean实现有如下bean:
配置数据源（mysql）
配置事务管理器
最后只需要
声明开启事务通过<tx>标签声明实现
session = HibernateUtil.getSession();//这是开启Session的操作
session.beginTransaction();     //这是开启Transaction的操作
session.save(user);            //这正是把数据放入一级缓存session中的操作
session.getTransaction().commit();//此处才是真正与数据库交互的语句
其次要注意hibernate.cfg.xml文件的编码格式与字符长度